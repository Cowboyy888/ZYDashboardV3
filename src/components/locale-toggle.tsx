'use client';
/**
 * @deprecated Superseded by <LanguageSwitch/> (the EN | 中文 segmented control).
 * Kept only as a re-export for backwards compatibility.
 */
export { LanguageSwitch as LocaleToggle } from '@/components/language-switch';
